# This install's values

Everything specific to this install lives here: hostnames, sizes, feature
switches. Nothing outside this directory names the client, so `platform/`,
`applications/` and `charts/` stay updatable from the scaffold.
