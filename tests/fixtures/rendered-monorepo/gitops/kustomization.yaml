# The entry point Flux reconciles. Order here is dependency order: policies and
# platform components exist before the applications that rely on them.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - policies
  - platform
  - applications
