# Tool caches and virtualenvs, mostly from running the fixture locally.
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# dbt
target/
dbt_packages/
logs/
*.duckdb

# The conformance audit writes its findings here for the issue-filing step.
# A run artefact, not source.
conformance-findings.json

# Written by tests/conformance_audit.py when run locally. The workflow uploads
# it as an artifact; a checked-in copy is a snapshot of one run pretending to
# be the current state of the fleet.
conformance-report.md
